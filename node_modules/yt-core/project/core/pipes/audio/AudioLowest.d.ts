/// <reference types="node" />
import { Readable } from "stream";
import type AudioFilters from "../../interface/AudioFilters";
type AudioFormat = "mp3" | "ogg" | "flac" | "aiff";
interface AudioLowestOC {
    query: string;
    stream?: boolean;
    verbose?: boolean;
    folderName?: string;
    outputFormat?: AudioFormat;
    filter?: keyof AudioFilters;
}
type ResultConfig = {
    status: number;
} | {
    message: string;
    status: number;
} | {
    stream: Readable;
    filename: string;
};
export type AudioLowestType = ResultConfig;
export default function AudioLowest({ query, filter, stream, verbose, folderName, outputFormat, }: AudioLowestOC): Promise<ResultConfig>;
export {};
//# sourceMappingURL=AudioLowest.d.ts.map