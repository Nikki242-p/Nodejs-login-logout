/// <reference types="node" />
import { Readable } from "stream";
type VideoFormat = "mp4" | "avi" | "mov";
interface AudioVideoLowestOC {
    query: string;
    stream?: boolean;
    verbose?: boolean;
    folderName?: string;
    outputFormat?: VideoFormat;
}
type ResultConfig = {
    status: number;
} | {
    message: string;
    status: number;
} | {
    stream: Readable;
    filename: string;
};
export type AudioVideoLowestType = ResultConfig;
export default function AudioVideoLowest({ query, stream, verbose, folderName, outputFormat, }: AudioVideoLowestOC): Promise<ResultConfig>;
export {};
//# sourceMappingURL=AudioVideoLowest.d.ts.map