/// <reference types="node" />
import { Readable } from "stream";
type VideoFormat = "mp4" | "avi" | "mov";
interface AudioVideoHighestOC {
    query: string;
    stream?: boolean;
    verbose?: boolean;
    folderName?: string;
    outputFormat?: VideoFormat;
}
type ResultConfig = {
    status: number;
} | {
    message: string;
    status: number;
} | {
    stream: Readable;
    filename: string;
};
export type AudioVideoHighestType = ResultConfig;
export default function AudioVideoHighest({ query, stream, verbose, folderName, outputFormat, }: AudioVideoHighestOC): Promise<ResultConfig>;
export {};
//# sourceMappingURL=AudioVideoHighest.d.ts.map