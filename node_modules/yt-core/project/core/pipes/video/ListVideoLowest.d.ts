/// <reference types="node" />
import { Readable } from "stream";
import type VideoFilters from "../../interface/VideoFilters";
type VideoFormat = "mp4" | "avi" | "mov";
interface ListVideoLowestOC {
    stream?: boolean;
    verbose?: boolean;
    folderName?: string;
    playlistUrls: string[];
    outputFormat?: VideoFormat;
    filter?: keyof VideoFilters;
}
type ResultConfig = {
    status: number;
} | {
    message: string;
    status: number;
} | {
    stream: Readable;
    filename: string;
};
export type ListVideoLowestType = ResultConfig;
export default function ListVideoLowest({ filter, stream, verbose, folderName, playlistUrls, outputFormat, }: ListVideoLowestOC): Promise<ListVideoLowestType[]>;
export {};
//# sourceMappingURL=ListVideoLowest.d.ts.map