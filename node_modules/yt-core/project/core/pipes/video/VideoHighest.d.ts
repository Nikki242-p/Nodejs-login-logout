/// <reference types="node" />
import { Readable } from "stream";
import type VideoFilters from "../../interface/VideoFilters";
type VideoFormat = "mp4" | "avi" | "mov";
interface VideoHighestOC {
    query: string;
    stream?: boolean;
    verbose?: boolean;
    folderName?: string;
    outputFormat?: VideoFormat;
    filter?: keyof VideoFilters;
}
type ResultConfig = {
    status: number;
} | {
    message: string;
    status: number;
} | {
    stream: Readable;
    filename: string;
};
export type VideoHighestType = ResultConfig;
export default function VideoHighest({ query, filter, stream, verbose, folderName, outputFormat, }: VideoHighestOC): Promise<ResultConfig>;
export {};
//# sourceMappingURL=VideoHighest.d.ts.map