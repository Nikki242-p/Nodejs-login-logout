/// <reference types="node" />
import { Readable } from "stream";
import type VideoFilters from "../../interface/VideoFilters";
type VideoFormat = "mp4" | "avi" | "mov";
interface VideoLowestOC {
    query: string;
    stream?: boolean;
    verbose?: boolean;
    folderName?: string;
    filter?: keyof VideoFilters;
    outputFormat?: VideoFormat;
}
type ResultConfig = {
    status: number;
} | {
    message: string;
    status: number;
} | {
    stream: Readable;
    filename: string;
};
export type VideoLowestType = ResultConfig;
export default function VideoLowest({ query, filter, stream, verbose, folderName, outputFormat, }: VideoLowestOC): Promise<ResultConfig>;
export {};
//# sourceMappingURL=VideoLowest.d.ts.map